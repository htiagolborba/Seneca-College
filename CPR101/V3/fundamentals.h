                                                        /* fundamentals module header version 1 */


#ifndef _FUNDAMENTALS_H_        // If the "fundamentals.h" file has been executed previously, it will not run again, as the "ifndef" (if not defined) check prevents it from being included multiple times.
#define _FUNDAMENTALS_H_	    // define _FUNDAMENTALS_H_ as to prevent reruns of the header file

#include <stdio.h>			    // include standard input/output lib
#include <string.h>			    // include the string manipulation lib
#include <stdlib.h>			    // include the standard library functions lib

void fundamentals(void);	    // prototype function declared here

#endif						    // end conditional definitions